apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "block-copy.fullname" . }}-test-connection"
  labels:
    {{- include "block-copy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "block-copy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
