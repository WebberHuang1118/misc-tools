apiVersion: v1
kind: Service
metadata:
  name: {{ include "block-copy.fullname" . }}
  labels:
    {{- include "block-copy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "block-copy.selectorLabels" . | nindent 4 }}
